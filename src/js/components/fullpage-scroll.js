// import { projectsSlider } from './sliders';

window.fullpage = new fullScroll({
  mainElement: 'page',
  displayDots: false,
  animateTime: 1,
  animateFunction: 'cubic-bezier(0.5, 0, 0.5, 1)',
});


